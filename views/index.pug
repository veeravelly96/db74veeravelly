extends layout

block content
  h1= title

  if (!user)
   a(href="/login") Login
   br
   br
   a(href="/register") Register
  if (user)
   p You are currently logged in as #{user.username}
   a(href="/phramacy") Take me to Phramacy
   br
   br
   a(href="/logout") Logout